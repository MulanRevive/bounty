func 冒泡(数列) {
  换过 = true
  while 换过 {
    换过 = false
    for i in range(len(数列) - 1) {
      if 数列[i] > 数列[i + 1] {
        数列[i], 数列[i + 1] = 数列[i + 1], 数列[i]
        换过 = true
      }
    }
  }
}

打乱数列 = [5, 2, 1, 8, 4]
冒泡(打乱数列)
print(打乱数列)