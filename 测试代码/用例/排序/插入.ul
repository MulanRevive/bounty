func 插入(数列) {
  for i in 1..<len(数列) {
    挑出 = 数列[i]
    位置 = i - 1
    while 位置 >= 0 and 数列[位置] > 挑出 {
      数列[位置 + 1] = 数列[位置]
      位置 -= 1
    }
    数列[位置 + 1] = 挑出
  }
}

打乱数列 = [5, 2, 1, 8, 4]
插入(打乱数列)
print(打乱数列)
